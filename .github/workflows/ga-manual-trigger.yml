name: Manual Triggering new
run-name: ${{ github.actor }} is testing out GitHub Actions. Manual 🚀
on: [push, workflow_dispatch]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 Workflow is triggered manually. 321"
